<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= article.persisted? ? "Edit Article" : "New Article" %></title>
  <!-- Incluir Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-4">
    <!-- Mostrar el título de la página según si se está creando o editando -->
    <h1><%= article.persisted? ? "Edit" : "Create" %></h1>

    <!-- Configurar el formulario con el modelo de artículo -->
    <%= form_with model: article, local: true, class: "mt-4" do |form| %>
      <!-- Campo para el título -->
      <div class="form-group">
        <%= form.label :title, class: "form-label" %>
        <%= form.text_field :title, class: "form-control", placeholder: "Enter article title" %>
        <!-- Mostrar errores para el campo del título -->
        <% article.errors.full_messages_for(:title).each do |message| %>
          <div class="text-danger mt-1"><%= message %></div>
        <% end %>
      </div>

      <!-- Campo para el cuerpo del artículo -->
      <div class="form-group">
        <%= form.label :body, class: "form-label" %>
        <%= form.text_area :body, class: "form-control", rows: 5, placeholder: "Enter article body" %>
        <!-- Mostrar errores para el campo del cuerpo -->
        <% article.errors.full_messages_for(:body).each do |message| %>
          <div class="text-danger mt-1"><%= message %></div>
        <% end %>
      </div>

      <!-- Botón para enviar el formulario -->
      <div class="form-group d-flex justify-content-between">
        <%= form.submit article.persisted? ? "Update Article" : "Create Article", class: "btn btn-primary" %>

        <!-- Botón de Cancelar para ambas pantallas -->
        <%= link_to "Cancel", articles_path, class: "btn btn-secondary" %>
      </div>
    <% end %>
  </div>

  <!-- Incluir Bootstrap JS y dependencias -->
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
